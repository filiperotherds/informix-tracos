BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[tracos_token] DROP CONSTRAINT [tracos_token_token_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
