BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[de_para_reserva] ADD CONSTRAINT de_para_reserva_pkey PRIMARY KEY CLUSTERED ([id]);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
